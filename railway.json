{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm install"]
    },
    "generate": {
      "commands": ["npm run generateClient"]
    }
  },
  "deploy": {
    "startCommand": "npm run start:prod"
  }
}